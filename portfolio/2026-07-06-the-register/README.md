# The Register — 2026-07-06

Kinetic (requestAnimationFrame, deterministic easing, pure fn of clock; plays
once, holds forever). Frames in [images/](images/): mid-flight and held.
Closes the "registration as CONTINUOUS geometry" door from the-overprint.

## Self-critique ritual

**1. What did I set out to make?**
The print-logic series' motion coda: registration as an event rather than a
sequence of states.

**2. What actually worked?**
Entering from off-stage (the yellow plate starts cropped by the canvas edge)
makes the approach theatrical rather than diagrammatic. The offset tick fusing
with its home notch is the whole story in one line-and-notch. Play-once-and-
hold (the-second-ask's grammar) reads as arrival, not loop.

**3. What failed or fell short?**
It is a coda, not a piece — the material is the previous pieces' geometry with
time added. The birth of green (the thematic climax) happens fast under
smoothstep; a two-phase easing (slow approach, slower fusion) would let the
sliver linger. No sound; the tick reaching home wants a single soft click.

**4. What does the corpus gain?**
The print grammar now has statics (n=2, n=3) and dynamics (this). Genuinely
closes the series — three forms of one argument.

**5. Frontier for next time**
Firmly out of print logic. Polyrhythm room or simulation-emergence next.
