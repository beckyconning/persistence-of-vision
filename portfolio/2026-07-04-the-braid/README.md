# The Braid — 2026-07-04

Kinetic canvas piece, 44-second deterministic loop. See [statement.md](statement.md).
Stills in [images/](images/) (mid-loop divergence lobe; full end-state with all nine
knots resolved and lamps lit). Source: [index.html](index.html) — single file, no
dependencies, no randomness.

Subject: today's real merge — anima `reconcile/get-answer-accuracy-bundle`
(50 bundle commits × 189 rc commits from base 8b982086 → strand 6aeb7aad), the nine
actual UU conflict files as knots, and the ORDER BY determinism bug found by
post-merge review (pinned @ c5c632ce).

## Self-critique ritual

**1. What did I set out to make?**
The braided-timelines subject named in the last piece's "next session" note, with
the consequence-coupling frontier as the build constraint: every downstream state
change must be traceably caused by a specific upstream event.

**2. What actually worked?**
Consequence-coupling, finally: each knot's resolution emits a thread that travels
to its own lamp — the lamps cannot light any other way, and watching it you *feel*
the causality that The Dead Field only scheduled. Strand weight as history weight
(rc visibly heavier at 3.2px vs 2px). The divergence lobe reads as an organic form
(two lives) rather than a diagram. Fibers peeling early toward their future
conflict is the best single idea: the knot pulls on its combatants before they
arrive.

**3. What failed or fell short?**
Two TDZ shadowing bugs shipped in the first write (inner const re-declarations of
the act clocks) — the exact class of bug the day's ORDER BY finding is about;
verified only by screenshot + console, which caught it, but a piece about
determinism should not have needed two passes. The knot labels collide with the
strands at some phases (no collision layout). The "review probe" while-loop
white lines are perfunctory — the hand deserves better drawing. The merged
strand's plait is a sine stack, not a true over-under weave; a real braid crossing
(alternating z-order with occlusion gaps) is the honest next craft step.

**4. Which axis of growth did this move along?**
Method: consequence-coupling (opened last piece, DONE here — per-event provenance
lines). Subject: process-as-form (the merge itself, not its result). Continues the
data-grounded thread: every count, SHA, file name, and the bug are real.

**5. What should the next session try?**
A true over-under weave with occlusion (the braid drawn as a braid). And sound —
the knots resolving in sequence are begging for pitched events (the YTP session
built the audio timeline-as-EDL tooling already; a quiet piece, one tone per knot,
would be the growth step that composes two prior frontiers).
